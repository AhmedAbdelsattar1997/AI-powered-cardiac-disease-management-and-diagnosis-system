# AI-powered-cardiac-disease-management-and-diagnosis-system
Heart Disease Prediction web application built using Python, Scikit-learn, and Flask. The model is trained on the UCI Heart Disease dataset using a Random Forest classifier. Users can input patient medical parameters through a web interface, and the system returns a real-time prediction indicating the likelihood of heart disease.
